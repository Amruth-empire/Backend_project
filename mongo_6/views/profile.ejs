<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Login</title>
    <link rel="stylesheet" href="/stylesheets/output.css">
</head>
<body class=" bg-gray-100">
    <div class=" w-full min-h-screen bg-zinc-900 text-white p-5 ">
  
        <div  class=" w-full flex justify-end">
            <a class="  bg-red-500 rounded-lg px-3 py-2 inline-block" href="/logout">Logout</a>
        </div>
        <div class=" flex items-start gap-3 ">
            
        <div class=" w-16 h-16 bg-red-500 rounded-md overflow-hidden">
            <img class="w-full h-full object-cover " src="/images/uploads/<%= user.profilepic %>" alt="">
        </div>
        <h2 class="text-3xl mb-3"><span class=" font-light">Hello</span>, <%= user.name.toUpperCase()%></h2>
        </div>
        <h4 class="mb-5">You can create a post new post </h4>
            
        <form method="post" action="/post">
            <textarea placeholder="Enter what ever you want.." class=" p-3 outline-none w-1/3 block  resize-none  bg-transparent border-2 border-zinc-800 rounded-lg" name="content" ></textarea>
            <input class=" px-3 py-2 w-40 bg-blue-500 block rounded-md mt-2 text-sm" type="submit" value="Create new post">
        </form>
    <div class=" posts mt-20">
        <h3 class="">Your Posts</h3>
        <div class="Postcontainer mt-5"> 
           <% user.posts.reverse().forEach(function(post){%>
            <div class=" post mb-4 w-1/3 p-3.5 border-[1px] border-zinc-800 rounded-lg bg-zinc-800">
                <h4 class="text-blue-500 mb-2" >@<%= user.username%></h4>
                <p class=" text-sm tracking-tight"><%= post.content%></p>
                <small class="mt-2 inline-block"><%= post.likes.length %>Likes</small>
                <div class=" btns flex gap-4 mt-5">
                    <a class="text-blue-500 " href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id) ===-1 ? "Like": "Unlike"%>
                    </a>
                    <a class="text-zinc-600 " href="/edit/<%= post._id%>">Edit</a>
                </div>
            </div>
            <% } )%>
        </div>
    </div>
    </div>
</body>
</html>
